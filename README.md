# tfjs-project

An introduction to AI in Node.js

## Requirements

- Node.js installed
- Python environment
- Xcode installed (MacOS only) xcode-select --install


How to run: 

1. `npm i`
2. `node .`
